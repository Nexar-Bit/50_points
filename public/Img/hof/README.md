# Hall of Fame promotional assets

Place generated images here using the filenames listed in `docs/HOF_ASSET_PROMPTS.md`.

The Hall of Fame entry wizard and achievement cards reference these paths via `src/frontend/lib/config/hofAssets.js`. Until images are added, the UI falls back to CSS effects and Lucide icons.
