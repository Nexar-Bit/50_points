# Leaderboard page assets

Place generated **PNG** images here using filenames in `docs/LEADERBOARD_ASSET_PROMPTS.md`.

Components load paths via `src/frontend/lib/config/leaderboardAssets.js`. The UI falls back to CSS when files are missing.

```bash
npm run verify:leaderboard-assets
```
